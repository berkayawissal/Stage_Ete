package com.example.demo.repository;

import com.example.demo.model.ChaineDeDistribution;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ChaineDeDistributionRepository extends JpaRepository<ChaineDeDistribution, Integer> {
}
